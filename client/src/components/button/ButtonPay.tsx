import { Button } from "@mui/material";

export default function ButtonPay() {
  return (
    <Button variant="contained" color="error" sx={{ borderRadius: "5px", width: "100%", height: "40px", fontWeight: "bold", boxShadow: "none" }}>
      Pay
    </Button>
  );
}

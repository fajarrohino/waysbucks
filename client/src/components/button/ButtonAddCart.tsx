import { Button } from "@mui/material";

export default function ButtonAddCart() {
  return (
    <Button variant="contained" color="error" sx={{ borderRadius: "5px", width: "576px", height: "40px", fontWeight: "bold", boxShadow: "none" }}>
      Add Cart
    </Button>
  );
}
